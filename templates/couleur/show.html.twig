{% extends 'base.html.twig' %}

{% block title %}Couleur{% endblock %}

{% block body %}
    <h1>Couleur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ couleur.id }}</td>
            </tr>
            <tr>
                <th>NomCouleur</th>
                <td>{{ couleur.nomCouleur }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary mt-3 mb-3" href="{{ path('app_couleur_index') }}">retour liste</a></br>

    <a class="btn btn-success mt-3 mb-3" href="{{ path('app_couleur_edit', {'id': couleur.id}) }}">modifier</a>

    {{ include('couleur/_delete_form.html.twig') }}
{% endblock %}
