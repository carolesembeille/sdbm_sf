{% extends 'base.html.twig' %}

{% block title %}Pays{% endblock %}

{% block body %}
    <h1>Pays</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pay.id }}</td>
            </tr>
            <tr>
                <th>NomPays</th>
                <td>{{ pay.nomPays }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary mt-3 mb-3" href="{{ path('app_pays_index') }}">retour liste</a></br>

    <a class="btn btn-info mt-3 mb-3" href="{{ path('app_pays_edit', {'id': pay.id}) }}">modifier</a>

    {{ include('pays/_delete_form.html.twig') }}
{% endblock %}
