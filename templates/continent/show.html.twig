{% extends 'base.html.twig' %}

{% block title %}Continent{% endblock %}

{% block body %}
    <h1>Continent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ continent.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ continent.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary mt-3 mb-3" href="{{ path('app_continent_index') }}">retour liste</a></br>

    <a class="btn btn-secondary mt-3 mb-3" href="{{ path('app_continent_edit', {'id': continent.id}) }}">modifier</a>

    {{ include('continent/_delete_form.html.twig') }}
{% endblock %}
