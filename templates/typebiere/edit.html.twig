{% extends 'base.html.twig' %}

{% block title %}Edit Typebiere
{% endblock %}

{% block body %}
	<h1>Modifier le Type de biere</h1>

	{{ include('typebiere/_form.html.twig', {'button_label': 'modifier'}) }}

	<a class="btn btn-secondary" href="{{ path('app_typebiere_index') }}">retour</a>

	{{ include('typebiere/_delete_form.html.twig') }}
{% endblock %}
