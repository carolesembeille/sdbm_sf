{% extends 'base.html.twig' %}

{% block title %}Typebiere
{% endblock %}

{% block body %}
	<h1>Liste des Types de bieres</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ typebiere.id }}</td>
			</tr>
			<tr>
				<th>NomType</th>
				<td>{{ typebiere.nomType }}</td>
			</tr>
		</tbody>
	</table>

	<a class="btn btn-secondary " href="{{ path('app_typebiere_index') }}">retour</a>
</br>

<a class="btn btn-info" href="{{ path('app_typebiere_edit', {'id': typebiere.id}) }}">modifier</a>

{{ include('typebiere/_delete_form.html.twig') }}{% endblock %}
